name: Restaurant Service CI

on:
  push:
    branches:
      - development
      - staging
      - main

  pull_request:
    branches:
      - development
      - staging
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Linter
        run: pnpm lint

      # Uncomment after adding a real test framework
      # - name: Run Unit Tests
      #   run: pnpm test

      - name: Build Application
        run: pnpm build

      - name: Build Docker Image
        run: docker build -t spotq-restaurant-service .